/** 全站对外展示名称（页眉、默认 og:site_name、各页 title 后缀等） */
export const SITE_DISPLAY_NAME = '影视精选';

/** 默认站点一句话描述（WebSite JSON-LD、可被 `PUBLIC_SITE_DESCRIPTION` 覆盖） */
export const SITE_DEFAULT_DESCRIPTION =
	'影视资讯、榜单与观影指引聚合阅读站，独立页面 URL 便于收录与分享链接预览。';
